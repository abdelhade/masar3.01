========================================
   وكيل طباعة المطبخ - دليل سريع
========================================

📋 الخطوات:

1️⃣  التجميع (مرة واحدة فقط)
   ▶ انقر نقراً مزدوجاً على: compile.bat
   ✅ سيتم إنشاء ملف PrintAgent.exe

2️⃣  التشغيل
   ▶ انقر نقراً مزدوجاً على: start-admin.bat
   ✅ سيعمل الخادم على http://localhost:5000

3️⃣  الاختبار
   ▶ افتح المتصفح: http://localhost:5000
   ▶ أو شغل: test-print.bat

========================================

🔧 ملاحظات مهمة:

• يجب تشغيل البرنامج كمسؤول (Administrator)
• تأكد من تثبيت الطابعات في Windows
• البرنامج يستخدم المنفذ 5000

========================================

📁 الملفات:

compile.bat       → تجميع البرنامج
start-admin.bat   → تشغيل كمسؤول (موصى به)
start.bat         → تشغيل عادي
test-print.bat    → اختبار الطباعة
README.md         → التوثيق الكامل

========================================

❓ مشاكل شائعة:

• "Access Denied"
  ➜ شغل start-admin.bat بدلاً من start.bat

• "الطابعة غير موجودة"
  ➜ افتح http://localhost:5000/printers
  ➜ استخدم الاسم الصحيح من القائمة

• "Port already in use"
  ➜ أغلق أي برنامج آخر يستخدم المنفذ 5000

========================================

✅ للتشغيل التلقائي عند بدء Windows:

1. اضغط Win + R
2. اكتب: shell:startup
3. انسخ اختصار PrintAgent.exe للمجلد

========================================
